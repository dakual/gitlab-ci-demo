apiVersion: v1
kind: Service
metadata:
  labels:
    app: ${DEPLY_NAME}
  name: ${DEPLY_NAME}
spec:
  ports:
  - nodePort: 30500
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: ${DEPLY_NAME}
  type: NodePort